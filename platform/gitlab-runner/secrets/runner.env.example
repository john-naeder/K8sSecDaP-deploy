# Copy sang runner.env rồi điền. runner.env đã bị .gitignore.
#
# runner_token là AUTHENTICATION TOKEN (bắt đầu bằng glrt-), lấy ở:
#   GitLab -> Admin -> CI/CD -> Runners -> New instance runner
# KHÔNG phải registration token kiểu cũ — GitLab 16+ đã bỏ luồng đó.
runner_token=
