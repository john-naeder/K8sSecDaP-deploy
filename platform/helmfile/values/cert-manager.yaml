installCRDs: true
replicaCount: 1
resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits:   { memory: 256Mi }
webhook:
  resources:
    requests: { cpu: 25m, memory: 32Mi }
    limits:   { memory: 128Mi }
cainjector:
  resources:
    requests: { cpu: 25m, memory: 64Mi }
    limits:   { memory: 256Mi }
