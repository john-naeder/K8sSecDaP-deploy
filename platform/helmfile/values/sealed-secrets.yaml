# Sealed-secrets controller — decrypt secrets at sync time so Git can
# carry encrypted payloads instead of plaintext.
resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits:   { memory: 256Mi }
metrics:
  serviceMonitor:
    enable: true
